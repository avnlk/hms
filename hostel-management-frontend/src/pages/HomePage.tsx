import { Typography } from "antd";

const HomePage = () => {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Title level={3}>Hostel Management System</Typography.Title>
      <Typography.Text>Authentication is initialized successfully.</Typography.Text>
    </div>
  );
};

export default HomePage;
